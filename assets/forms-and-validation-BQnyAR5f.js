var e=[{id:`forms-and-validation/react-hook-form`,blocks:[{type:`text`,lead:!0,text:{en:`Controlled inputs re-render the whole form on every keystroke. That is fine for two fields and noticeable at twenty. React Hook Form keeps inputs **uncontrolled** and subscribes only the parts that need to know.`,ar:`الحقول المُتحكَّم بها تُعيد عرض النموذج كله عند كل ضغطة. وهذا مقبول لحقلين وملحوظ عند عشرين. يُبقي React Hook Form الحقول **غير متحكَّم بها** ويشترك فقط بما يحتاج المعرفة.`}},{type:`code`,lang:`tsx`,code:`const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm();

<form onSubmit={handleSubmit(onSubmit)}>
  <input {...register('email', { required: 'Email is required' })} />
  {errors.email && <span role="alert">{errors.email.message}</span>}

  <button disabled={isSubmitting}>
    {isSubmitting ? 'Saving…' : 'Save'}
  </button>
</form>`},{type:`list`,items:{en:["`register` wires the input directly to the DOM — no `value`, no `onChange`, no re-render per character.","`handleSubmit` validates first and only calls your function with valid, typed data.","`formState` gives you `errors`, `isSubmitting`, `isDirty` and `isValid` without extra state.","For custom components that cannot take a ref, `<Controller>` bridges the gap."],ar:["`register` يربط الحقل بـ DOM مباشرة — بلا `value` ولا `onChange` ولا إعادة عرض لكل حرف.","`handleSubmit` يتحقّق أولًا ولا يستدعي دالتك إلا ببيانات صالحة ومُنمَّطة.","`formState` يمنحك `errors` و `isSubmitting` و `isDirty` و `isValid` بلا حالة إضافية.","وللمكوّنات المخصّصة التي لا تقبل مرجعًا، يسدّ `<Controller>` الفجوة."]}},{type:`callout`,tone:`tip`,body:{en:'Need to react to one field — showing a section only when a checkbox is ticked? Use `watch("field")` for that field alone rather than making the whole form controlled again.',ar:'تحتاج التفاعل مع حقل واحد — كإظهار قسم عند تأشير مربّع؟ استخدم `watch("field")` لذلك الحقل وحده بدل جعل النموذج كله متحكَّمًا به من جديد.'}},{type:`quiz`,question:{en:`Why is an uncontrolled form faster on a large page?`,ar:`لماذا يكون النموذج غير المتحكَّم به أسرع في صفحة كبيرة؟`},options:[{text:{en:`Keystrokes stay in the DOM instead of triggering a React render for the whole form.`,ar:`لأن الضغطات تبقى في DOM بدل إطلاق عرض في رياكت للنموذج كله.`},correct:!0},{text:{en:`It validates on the server instead of the client.`,ar:`لأنه يتحقّق على الخادم بدل العميل.`}},{text:{en:`It renders fewer DOM nodes.`,ar:`لأنه يعرض عقد DOM أقل.`}},{text:{en:`It disables React’s reconciliation.`,ar:`لأنه يعطّل المطابقة في رياكت.`}}],explain:{en:"The browser already stores what the user typed. React only needs the value at submit time — or when you explicitly `watch` a field.",ar:`المتصفّح يخزّن ما كتبه المستخدم أصلًا. ولا تحتاج رياكت القيمة إلا عند الإرسال — أو حين تراقب حقلًا صراحةً.`}},{type:`keypoints`,items:{en:["`register` connects a field with no controlled state.","`handleSubmit` runs validation before your handler.","`formState` replaces your own loading and error flags.","`watch` a single field instead of controlling everything."],ar:["`register` يربط الحقل بلا حالة متحكَّم بها.","`handleSubmit` يشغّل التحقّق قبل معالجك.","`formState` يغني عن راياتك للتحميل والأخطاء.",`راقب حقلًا واحدًا بدل التحكّم بكل شيء.`]}}]},{id:`forms-and-validation/zod`,blocks:[{type:`text`,lead:!0,text:{en:`TypeScript disappears at runtime, so it cannot protect you from a malformed API response or a user pasting nonsense. Zod validates at runtime **and** hands the matching TypeScript type back to you.`,ar:`يختفي تايب سكربت وقت التشغيل، فلا يحميك من استجابة مشوّهة أو من مستخدم يلصق كلامًا بلا معنى. أما Zod فيتحقّق وقت التشغيل **ويمنحك** النوع المطابق في تايب سكربت.`}},{type:`code`,lang:`ts`,code:`import { z } from 'zod';

const signupSchema = z.object({
  email: z.string().email('Enter a valid email'),
  age: z.number().int().min(18, 'You must be 18 or older'),
  password: z.string().min(8, 'At least 8 characters'),
  confirm: z.string(),
}).refine((data) => data.password === data.confirm, {
  message: 'Passwords do not match',
  path: ['confirm'],            // attach the error to the right field
});

type Signup = z.infer<typeof signupSchema>;   // the type, for free`},{type:`callout`,tone:`tip`,title:{en:`One schema, three jobs`,ar:`مخطّط واحد بثلاث وظائف`},body:{en:`The same schema validates the form, validates the request body on the server, and produces the TypeScript type both sides share. Change a rule once and everything follows.`,ar:`المخطّط نفسه يتحقّق من النموذج، ويتحقّق من جسم الطلب على الخادم، وينتج نوع تايب سكربت الذي يتشاركه الطرفان. غيّر قاعدة مرة فيتبعها كل شيء.`}},{type:`code`,lang:`ts`,code:`// parse throws on failure
const user = userSchema.parse(await res.json());

// safeParse returns a result you can branch on
const result = userSchema.safeParse(input);
if (!result.success) {
  console.log(result.error.issues);   // field-level messages
}`},{type:`quiz`,question:{en:`Why is validating an API response with Zod worth the extra code?`,ar:`لماذا يستحق التحقّق من استجابة الواجهة بـ Zod الكود الإضافي؟`},options:[{text:{en:`A TypeScript type is only a promise about the response; Zod actually checks it and fails at the boundary instead of deep inside a component.`,ar:`لأن نوع تايب سكربت مجرّد وعد عن الاستجابة، بينما يفحصها Zod فعلًا ويفشل عند الحدّ لا في عمق مكوّن.`},correct:!0},{text:{en:`It makes the request faster.`,ar:`يجعل الطلب أسرع.`}},{text:{en:`It replaces the need for error boundaries.`,ar:`يغني عن حدود الأخطاء.`}},{text:{en:`TypeScript cannot type API responses at all.`,ar:`لا يستطيع تايب سكربت تنميط استجابات الواجهات إطلاقًا.`}}],explain:{en:"Casting a response with `as User` is a wish, not a check. When the API renames a field, you find out from a blank screen — or from Zod, with the exact path that failed.",ar:"تحويل الاستجابة بـ `as User` أمنية لا فحص. وعند تغيير الواجهة لاسم حقل تكتشف ذلك من شاشة فارغة — أو من Zod مع المسار الذي فشل بالضبط."}},{type:`keypoints`,items:{en:["Zod validates at runtime; `z.infer` gives you the static type.","Use `.refine` for rules that span several fields.","`safeParse` when you want to handle failure, `parse` when you want to throw.",`Validate at every boundary: forms, API responses, URL params.`],ar:["Zod يتحقّق وقت التشغيل، و`z.infer` يمنحك النوع الساكن.","استخدم `.refine` للقواعد التي تمتدّ عبر عدة حقول.","`safeParse` حين تريد معالجة الفشل، و`parse` حين تريد رمي خطأ.",`تحقّق عند كل حدّ: النماذج واستجابات الواجهات ومعاملات الروابط.`]}}]},{id:`forms-and-validation/rhf-zod-resolver`,blocks:[{type:`text`,lead:!0,text:{en:`Wire the two together with a resolver and the form becomes fully typed: field names are checked, error messages come from the schema, and the submitted data arrives as the exact type you declared.`,ar:`اربط الاثنين عبر مُحلِّل فيصبح النموذج مُنمَّطًا بالكامل: تُفحص أسماء الحقول، وتأتي رسائل الخطأ من المخطّط، وتصل البيانات المُرسَلة بالنوع الذي أعلنته بالضبط.`}},{type:`code`,lang:`tsx`,code:`import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';

const schema = z.object({
  email: z.string().email('Enter a valid email'),
  password: z.string().min(8, 'At least 8 characters'),
});
type FormValues = z.infer<typeof schema>;

function LoginForm() {
  const {
    register, handleSubmit, setError,
    formState: { errors, isSubmitting },
  } = useForm<FormValues>({ resolver: zodResolver(schema) });

  async function onSubmit(values: FormValues) {   // already validated
    const res = await login(values);
    if (!res.ok) setError('root', { message: 'Wrong email or password' });
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} noValidate>
      <input {...register('email')} aria-invalid={!!errors.email} />
      {errors.email && <span role="alert">{errors.email.message}</span>}

      <input type="password" {...register('password')} />
      {errors.password && <span role="alert">{errors.password.message}</span>}

      {errors.root && <p role="alert">{errors.root.message}</p>}
      <button disabled={isSubmitting}>Sign in</button>
    </form>
  );
}`},{type:`list`,items:{en:['Typos in `register("emial")` become compile errors, because the field names come from the schema.',"Validation lives in one place — the schema — not scattered across `required` props.",'`setError("root")` is where server-side failures belong, next to field errors.',"Add `noValidate` so your messages show instead of the browser’s default bubbles."],ar:['أي خطأ إملائي في `register("emial")` يصبح خطأ تصريف، لأن أسماء الحقول تأتي من المخطّط.',"التحقّق في مكان واحد — المخطّط — لا مبعثرًا في خصائص `required`.",'`setError("root")` هو مكان أخطاء الخادم بجوار أخطاء الحقول.',"أضف `noValidate` لتظهر رسائلك بدل فقاعات المتصفّح الافتراضية."]}},{type:`callout`,tone:`warn`,body:{en:`Client validation is a courtesy, never a defence. Run the same schema on the server — a request can arrive from anything, not just your form.`,ar:`التحقّق في العميل مجاملة لا دفاع. شغّل المخطّط نفسه على الخادم — فالطلب قد يأتي من أي شيء لا من نموذجك فقط.`}},{type:`quiz`,question:{en:`Where should a "this email is already registered" error be shown?`,ar:`أين يجب عرض خطأ «هذا البريد مسجَّل مسبقًا»؟`},options:[{text:{en:'On the email field via `setError("email", …)` after the server responds.',ar:'على حقل البريد عبر `setError("email", …)` بعد استجابة الخادم.'},correct:!0},{text:{en:`Only in a toast notification.`,ar:`في إشعار عائم فقط.`}},{text:{en:"In the Zod schema, as a `.refine` rule.",ar:"في مخطّط Zod كقاعدة `.refine`."}},{text:{en:`In the browser console.`,ar:`في طرفية المتصفّح.`}}],explain:{en:`Only the server knows what already exists, so the schema cannot check it. Attaching the message to the field puts the fix where the user is looking.`,ar:`الخادم وحده يعرف ما هو موجود، فلا يستطيع المخطّط فحصه. وإلحاق الرسالة بالحقل يضع الحل حيث ينظر المستخدم.`}},{type:`keypoints`,items:{en:["`zodResolver` connects schema validation to the form.","`useForm<FormValues>` makes field names and data type-safe.","Server errors go to a field, or to `root` for form-level failures.",`Always re-validate on the server with the same schema.`],ar:["`zodResolver` يربط تحقّق المخطّط بالنموذج.","`useForm<FormValues>` يجعل أسماء الحقول والبيانات آمنة الأنواع.","أخطاء الخادم تذهب إلى حقل، أو إلى `root` لأخطاء النموذج ككل.",`أعد التحقّق دائمًا على الخادم بالمخطّط نفسه.`]}}]},{id:`forms-and-validation/complex-fields`,blocks:[{type:`text`,lead:!0,text:{en:`Beyond a login box, forms grow rows you can add and remove, checks that need the server, and steps. Each of those has an accessibility requirement that is easy to miss and impossible to ignore once you notice it.`,ar:`بعد صندوق تسجيل الدخول، تنمو النماذج بصفوف تُضاف وتُحذف، وفحوص تحتاج الخادم، وخطوات. ولكلٍّ منها متطلّب إتاحة يسهل إغفاله ويستحيل تجاهله بعد ملاحظته.`}},{type:`code`,lang:`tsx`,code:`const { fields, append, remove } = useFieldArray({ control, name: 'guests' });

{fields.map((field, index) => (
  <div key={field.id}>                      {/* field.id, never index */}
    <input {...register(\`guests.\${index}.name\`)} />
    <button type="button" onClick={() => remove(index)}>Remove</button>
  </div>
))}
<button type="button" onClick={() => append({ name: '' })}>Add guest</button>`},{type:`table`,head:{en:[`Requirement`,`How`],ar:[`المتطلّب`,`الطريقة`]},rows:[{en:[`Label every input`,"`<label htmlFor>` — a placeholder is not a label"],ar:[`تسمية كل حقل`,"`<label htmlFor>` — والنص النائب ليس تسمية"]},{en:[`Announce errors`,'`role="alert"` and `aria-invalid`'],ar:[`إعلان الأخطاء`,'`role="alert"` و `aria-invalid`']},{en:[`Link error to field`,'`aria-describedby="email-error"`'],ar:[`ربط الخطأ بالحقل`,'`aria-describedby="email-error"`']},{en:[`Focus the first error`,"`setFocus()` after a failed submit"],ar:[`التركيز على أول خطأ`,"`setFocus()` بعد إرسال فاشل"]},{en:[`Never disable submit silently`,`Let them submit and explain what is wrong`],ar:[`لا تعطّل الإرسال بصمت`,`دعهم يرسلون واشرح الخطأ`]}]},{type:`callout`,tone:`warn`,title:{en:`Validate at the right moment`,ar:`تحقّق في اللحظة الصحيحة`},body:{en:`Showing "invalid email" after the first typed character is hostile. Validate a field on blur, then re-validate on change once it has already failed — the user gets feedback exactly when it helps.`,ar:`إظهار «بريد غير صالح» بعد أول حرف عدائي. تحقّق من الحقل عند مغادرته، ثم أعد التحقّق مع الكتابة بعد أن يفشل مرة — فيحصل المستخدم على الملاحظة حين تفيده تمامًا.`}},{type:`list`,items:{en:[`Multi-step forms: keep all values in one form state and validate the current step’s subset before advancing.`,`Async checks such as "is this username free" should be debounced and cancellable.`,`Show a summary of errors at the top of long forms, with links that focus the offending field.`,`Keep the submit button enabled — a disabled button with no explanation is a dead end.`],ar:[`النماذج متعدّدة الخطوات: احتفظ بكل القيم في حالة نموذج واحدة وتحقّق من مجموعة الخطوة الحالية قبل التقدّم.`,`الفحوص غير المتزامنة مثل «هل هذا الاسم متاح» يجب أن تكون مؤجَّلة وقابلة للإلغاء.`,`اعرض ملخّص أخطاء أعلى النماذج الطويلة مع روابط تُركّز على الحقل المعني.`,`أبقِ زر الإرسال مفعّلًا — فزر معطّل بلا تفسير طريق مسدود.`]}},{type:`quiz`,question:{en:"Why should a field array use `field.id` as the key instead of the index?",ar:"لماذا يجب أن يستخدم مصفوفة الحقول `field.id` كمفتاح بدل الفهرس؟"},options:[{text:{en:`Removing a row shifts the indices, so React would keep the wrong DOM node and the typed values would move to the wrong row.`,ar:`لأن حذف صف يزيح الفهارس، فتحتفظ رياكت بعقدة DOM الخاطئة وتنتقل القيم المكتوبة إلى الصف الخطأ.`},correct:!0},{text:{en:"Because `field.id` is shorter.",ar:"لأن `field.id` أقصر."}},{text:{en:`Because indexes are not allowed as keys in TypeScript.`,ar:`لأن الفهارس غير مسموحة كمفاتيح في تايب سكربت.`}},{text:{en:`It makes no difference here.`,ar:`لا فرق هنا.`}}],explain:{en:`It is the index-as-key bug from phase 2, in the place where it hurts most — a user deletes row two and watches their data jump into row three.`,ar:`إنه خلل الفهرس-كمفتاح من المرحلة الثانية، في المكان الأكثر إيلامًا — يحذف المستخدم الصف الثاني فيرى بياناته تقفز إلى الثالث.`}},{type:`keypoints`,items:{en:["`useFieldArray` for dynamic rows — key on `field.id`.",`Validate on blur, then on change after the first failure.`,'Labels, `role="alert"` and `aria-describedby` are not optional.',`Focus the first invalid field after a failed submit.`],ar:["`useFieldArray` للصفوف الديناميكية — والمفتاح `field.id`.",`تحقّق عند المغادرة ثم مع الكتابة بعد أول فشل.`,'التسميات و `role="alert"` و `aria-describedby` ليست اختيارية.',`ركّز على أول حقل غير صالح بعد إرسال فاشل.`]}}]}];export{e as formsAndValidation};