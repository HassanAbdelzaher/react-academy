import type { ExamQuestion } from './types'

/**
 * Intermediate exam bank — phases 4-9 (hooks in depth, how React really works,
 * TypeScript, tooling, styling, routing and data).
 *
 * Not yet written. An empty bank makes the level show as "in preparation" on
 * the exam page rather than offering a sitting it cannot fill.
 */
export const intermediateBank: ExamQuestion[] = []
